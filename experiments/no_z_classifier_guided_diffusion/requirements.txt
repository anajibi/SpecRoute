# Runtime requirements for the no-Z classifier-guided diffusion POC.
#
# For CUDA-specific PyTorch wheels, follow https://pytorch.org/get-started/locally/
# if plain `pip install -r requirements.txt` does not select the desired CUDA build.
torch>=2.1
torchvision>=0.16

# Hugging Face Diffusers stack for google/ddpm-celebahq-256, DDIMScheduler,
# DDIMInverseScheduler, and model/checkpoint loading.
diffusers>=0.29
accelerate>=0.30
transformers>=4.40
huggingface-hub>=0.23
safetensors>=0.4

# Data loading, config parsing, metrics, and image/grid writing.
numpy>=1.24
PyYAML>=6.0
Pillow>=10.0
